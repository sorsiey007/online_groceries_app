import 'package:flutter/material.dart';

class MyAppTheme {
  // Main accent color (used for buttons, highlights)
  static const Color mainColor = Color(0xFFEB5E28); // Accent Color
  
  // Light theme text color
  static const Color primaryColor = Color(0xFFFFFFFF); // Text Color (Light Theme)
  
  // Secondary text color with 50% opacity
  static const Color secondaryColor = Color(0x80FFFFFF); // 50% opacity for text
  
  // Border color with 12% opacity
  static const Color borderColor12 = Color(0x80000000); // Border Color with 12% opacity
  
  // Background color (dark theme)
  static const Color backgroundColor = Color(0xFF000000); // Background color

  // Error color (red, used for error messages or invalid states)
  static const Color errorColor = Color(0xFFFF0000); // Error Color
  
  // Success color (green, used for success messages or confirmations)
  static const Color successColor = Color(0xFF28A745); // Success Color
  
  // Information color (blue, used for informational messages)
  static const Color infoColor = Color(0xFF17A2B8); // Info Color
  
  // Hover color (lighter tone for interactive elements)
  static const Color hoverColor = Colors.blueAccent; // Hover color
  
  // Focus color (used for focused UI elements like text fields or buttons)
  static const Color focusColor = Colors.blue; // Focus Color
  
  // Disabled color (light gray, for disabled UI elements)
  static const Color disabledColor = Colors.grey; // Disabled Color
  
  // Divider color (light gray for dividers between elements)
  static const Color dividerColor = Color(0xFFDCDCDC); // Divider Color
  
  // Accent color for small highlights or badges
  static const Color accentColor = Color(0xFFFFA500); // Accent Color
  
  // Overlay color for blurred or dimmed overlays
  static const Color overlayColor = Color(0x80EB5E28); // Overlay Color with opacity

  // Light background color for cards or containers
  static const Color cardBackgroundColor = Color(0xFFFFFFFF); // Card background color (Light theme)

  // Text color for dark theme (default for most text)
  static const Color darkTextColor = Color(0xFF212121); // Dark text color for dark backgrounds

  // Text color for light theme (default for most text)
  static const Color lightTextColor = Color(0xFF000000); // Light text color for light backgrounds
}
